# Super Puper Quiz

Велком ту Супер-пупер квіз. Проект, написаний на Next.js з дотриманням всіх вимог, що вказані в проектній документації. Та навіть трохи більше

## Запуск проекту

1.  **Встановіть залежності**:

    ```
    npm install
    # або
    yarn install
    # або
    pnpm install
    ```

2.  **Запустіть сервер розробки**:

    ```
    npm run dev
    ```

3.  **Відкрийте в браузері**:
    Перейдіть за посиланням [http://localhost:3000](http://localhost:3000).

## 📂 Структура Проекту

Архітектура побудована за принципом **Feature-based**, що полегшує масштабування. Папка `app` лишається чистою та використовується лише для навігації (маршрутизації); вся бізнес-логіка та UI збираються в `features/`.

```
app/              # Next.js App Router (сторінки та layouts) — тільки навігація
components/       # Загальні UI компоненти (кнопки, інпути)
context/          # Глобальний стан (QuizProvider)
features/         # Логіка та компоненти, специфічні для кожної фічі (quiz, analyzing, email)
lib/              # Утиліти, константи, хелпери
locales/          # Файли перекладів (i18n)
types/            # TypeScript типи
```

## Limitations

- **Клієнтська логіка генерації вихідного документу**: Генерація результатів та CSV файлів відбувається повністю на стороні клієнта (Browser-side), що зменшує навантаження на сервер. Генерацію документа можна перенести на серверну сторону, але в цьому немає практичного сенсу — операція легка (конкатенація рядків), і виконання її в браузері миттєве.
- **Залежність даних**: Список улюблених тем (Крок 5) залежить від обраного віку. Якщо користувач змінить вікову групу, несумісні теми будуть автоматично видалені з вибору.
- **Перехід через крок по URL**: Зараз немає механізму перенаправити користувача, який спробує «перескочити» крок, ввівши в адресному рядку номер іншого кроку (наприклад, `/quiz/5` без проходження попередніх).

## Additional notes

- **Варіанти на екрані 5 залежать від екрану 3**: Варіанти вибору на п'ятому екрані (улюблені теми) відрізняються залежно від того, що користувач обрав на третьому екрані (вікова група). Для кожної вікової категорії показується свій набір тем.
- **Без memo / useMemo / useCallback**: Ці оптимізації не використовувались. Дерево компонентів невелике, немає важких обчислень і масових ре-рендерів, дані квізу прості. Для поточних бізнес-вимог продуктивність і так достатня, тому додаткове мемоїзування не потрібне і лише ускладнило б код без видимої користі.
- **Розділення логіки екранів опитування (клієнт / сервер)**: Для оптимізації логіка всередині екранів квізу розділена на серверну та клієнтську частини: `index` — серверний компонент (дані, конфіг), `options` — клієнтський (інтерактив, стан).
- **Вимкнено лінтер у репозиторії**: Конфіг або правила лінтера додано в `.gitignore`, щоб налаштування IDE та кастомні правила інших розробників не конфліктували з проектом.
